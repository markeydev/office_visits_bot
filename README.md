# Telegram бот для отслеживания посещений офиса

Бот для отметки посещений офиса с учетом рабочего календаря России 2026. Помогает соблюдать требование посещать офис минимум 1 раз в рабочую неделю.

## 🎯 Функционал

- ✅ Отметка посещений офиса (был в офисе / работал удаленно)
- 📅 Учет рабочего календаря России 2026 (праздники, выходные)
- 📊 Статистика по неделям и месяцам
- 📈 Генерация отчетов (текстовый + графический календарь)
- 🔔 Контроль выполнения требования (1 визит в неделю)

## 📋 Команды бота

- `/start` - Начать работу с ботом
- `/today` - Отметить сегодняшний день
- `/week` - Показать статус текущей недели
- `/report` - Сформировать отчет за месяц

## 🚀 Развертывание на Linux

### Предварительные требования

1. **Docker и Docker Compose**

```bash
# Установка Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# Установка Docker Compose
sudo apt-get update
sudo apt-get install docker-compose-plugin

# Проверка установки
docker --version
docker compose version
```

2. **Токен Telegram бота**

- Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
- Отправьте команду `/newbot`
- Следуйте инструкциям для создания бота
- Сохраните полученный токен

### Установка и запуск

1. **Клонируйте или загрузите проект**

```bash
cd /opt
sudo mkdir office-visits-bot
cd office-visits-bot
```

2. **Скопируйте все файлы проекта в эту директорию:**

- bot.py
- database.py
- calendar_data.py
- report_generator.py
- requirements.txt
- Dockerfile
- docker-compose.yml
- .env.example

3. **Создайте файл .env с вашим токеном**

```bash
cp .env.example .env
nano .env
```

Замените `your_bot_token_here` на ваш реальный токен от BotFather:

```
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
```

Сохраните файл (Ctrl+O, Enter, Ctrl+X)

4. **Создайте директорию для базы данных**

```bash
mkdir -p data
```

5. **Соберите и запустите контейнер**

```bash
# Сборка образа
sudo docker compose build

# Запуск в фоновом режиме
sudo docker compose up -d
```

6. **Проверьте статус**

```bash
# Проверка запущенных контейнеров
sudo docker compose ps

# Просмотр логов
sudo docker compose logs -f
```

### 🔧 Управление ботом

**Остановить бота:**
```bash
cd /opt/office-visits-bot
sudo docker compose stop
```

**Запустить бота:**
```bash
cd /opt/office-visits-bot
sudo docker compose start
```

**Перезапустить бота:**
```bash
cd /opt/office-visits-bot
sudo docker compose restart
```

**Остановить и удалить контейнер:**
```bash
cd /opt/office-visits-bot
sudo docker compose down
```

**Пересобрать после изменений:**
```bash
cd /opt/office-visits-bot
sudo docker compose down
sudo docker compose build --no-cache
sudo docker compose up -d
```

**Просмотр логов:**
```bash
cd /opt/office-visits-bot

# Все логи
sudo docker compose logs

# Последние 100 строк
sudo docker compose logs --tail=100

# Следить за логами в реальном времени
sudo docker compose logs -f
```

### 📊 Резервное копирование

База данных SQLite хранится в директории `./data/office_visits.db`

**Создать резервную копию:**
```bash
cd /opt/office-visits-bot
sudo cp data/office_visits.db data/office_visits_backup_$(date +%Y%m%d_%H%M%S).db
```

**Автоматическое резервное копирование (cron):**
```bash
sudo crontab -e
```

Добавьте строку (резервная копия каждый день в 3:00):
```
0 3 * * * cp /opt/office-visits-bot/data/office_visits.db /opt/office-visits-bot/data/office_visits_backup_$(date +\%Y\%m\%d).db
```

### 🔒 Безопасность

1. **Установите правильные права доступа:**

```bash
cd /opt/office-visits-bot
sudo chmod 600 .env
sudo chmod 755 data
```

2. **Используйте firewall для ограничения доступа:**

```bash
sudo ufw enable
sudo ufw allow ssh
# Бот не требует открытых портов, работает только исходящие соединения
```

### 📱 Автозапуск при перезагрузке

Docker Compose автоматически настроен на перезапуск контейнера (`restart: unless-stopped`), но убедитесь, что Docker запускается при старте системы:

```bash
sudo systemctl enable docker
```

### 🐛 Решение проблем

**Бот не отвечает:**
```bash
# Проверьте логи
sudo docker compose logs --tail=50

# Проверьте статус контейнера
sudo docker compose ps

# Перезапустите бота
sudo docker compose restart
```

**Ошибка "Cannot connect to the Docker daemon":**
```bash
# Запустите Docker
sudo systemctl start docker

# Добавьте пользователя в группу docker (опционально)
sudo usermod -aG docker $USER
# После этого нужно перелогиниться
```

**База данных повреждена:**
```bash
# Остановите бота
sudo docker compose stop

# Восстановите из резервной копии
cd /opt/office-visits-bot/data
sudo cp office_visits_backup_YYYYMMDD.db office_visits.db

# Запустите бота
sudo docker compose start
```

### 📈 Мониторинг

**Использование ресурсов:**
```bash
sudo docker stats office-visits-bot
```

**Размер базы данных:**
```bash
ls -lh /opt/office-visits-bot/data/office_visits.db
```

### 🔄 Обновление бота

При выходе новой версии:

```bash
cd /opt/office-visits-bot

# Остановите текущую версию
sudo docker compose down

# Обновите файлы (bot.py и др.)
# ...

# Пересоберите и запустите
sudo docker compose build
sudo docker compose up -d
```

## 📂 Структура проекта

```
office-visits-bot/
├── bot.py                  # Основной файл бота
├── database.py             # Работа с базой данных
├── calendar_data.py        # Рабочий календарь России 2026
├── report_generator.py     # Генерация отчетов
├── requirements.txt        # Python зависимости
├── Dockerfile             # Docker образ
├── docker-compose.yml     # Docker Compose конфигурация
├── .env                   # Переменные окружения (токен)
├── .env.example          # Пример файла .env
└── data/                 # Директория для базы данных
    └── office_visits.db  # SQLite база данных
```

## 💡 Использование

1. Найдите своего бота в Telegram по username
2. Отправьте `/start`
3. Используйте кнопки для отметки посещений
4. Просматривайте статистику и отчеты

## 🎨 Особенности

- Автоматический учет праздников и выходных дней
- Красивые визуальные отчеты-календари
- Напоминания о необходимости посещения офиса
- Статистика по неделям с цветовой индикацией
- Сохранение всех данных в локальной базе данных

## 📝 Лицензия

MIT License

## 👨‍💻 Поддержка

При возникновении проблем:
1. Проверьте логи: `sudo docker compose logs`
2. Убедитесь, что токен бота правильно указан в `.env`
3. Проверьте, что Docker запущен: `sudo systemctl status docker`
